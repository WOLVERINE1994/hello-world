#unique number from the list
from random import randint
list=[]
for i in range(10):
    list.append(randint(1,10))
print(list)
def unique_list(list):
    list1=[]
    for i in list:
        if(i not in list1):
            list1.append(i)
    return (list1)
unique_list(list)
